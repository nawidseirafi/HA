google-genai
openai
playwright
python-dotenv
PyYAML
pypdf
requests
# OCR-Fallback fuer Bild-PDFs und Bild-Belege (optional, aber empfohlen).
# Benoetigt zusaetzlich das Tesseract-Binary (z.B. `winget install UB-Mannheim.TesseractOCR`
# bzw. `brew install tesseract tesseract-lang`) und Poppler fuer pdf2image
# (`brew install poppler` / Windows: https://github.com/oschwartz10612/poppler-windows).
# Alternativ statt pdf2image+Poppler: PyMuPDF (siehe unten).
#pip install pytesseract Pillow pdf2image PyMuPDF
pytesseract
Pillow
pdf2image
# Fallback-Renderer ohne Poppler-Binary:
PyMuPDF
# Formatiertes XLSX fuer Steuer-Export (Header, Filter, Summen, Datums-/EUR-Format).
# Ohne openpyxl wird ein minimaler XLSX-Writer als Fallback genutzt.
openpyxl
