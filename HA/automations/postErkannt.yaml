alias: Post erkannt speichern
mode: single
trigger:
  - platform: state
    entity_id: binary_sensor.briefkasten
    to: "on"
action:
  - service: input_boolean.turn_on
    target:
      entity_id: input_boolean.post_im_briefkasten