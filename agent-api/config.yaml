server:
  host: 0.0.0.0
  port: 8080

storage:
  uploads_dir: storage/uploads
  status_file: storage/status.json

logging:
  file: logs/agent-api.log
  level: INFO

agents:
  invoices:
    enabled: true
    upload_dir: storage/uploads/invoices
  vacation:
    enabled: true
